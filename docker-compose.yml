version: '3.8'

services:
  querymind-ai-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: querymind-ai-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
